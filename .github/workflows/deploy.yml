name: Deploy Storefront

on:
  push:
    branches:
      - vedashi-herbals-prod

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Execute Remote SSH Commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "Deploying Storefront..."
            cd ~/vedashi/Storefront-Vedashi
            git fetch origin
            git reset --hard origin/${{ github.ref_name }}
            
            cd ~/vedashi
            # Use flock to prevent simultaneous Docker builds across repositories
            flock -w 600 /tmp/vedashi-deploy.lock -c "docker compose up -d --build storefront"
